{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // ⚠️ WARNING: DEVELOPMENT ONLY - DO NOT USE IN PRODUCTION ⚠️
  // This connection string contains hardcoded credentials and is intended for local development and testing only.
  // NEVER commit this configuration to production environments or public codebases.
  // For production, use environment variables, Azure Key Vault, or other secure secret management solutions.
  // Remove or secure this connection string before deploying to production environments.
  "ConnectionStrings": {
    "Default": "Server=tcp:orkinosai.database.windows.net,1433;Initial Catalog=sitechat;Persist Security Info=False;User ID=sqladmin;Password=Sarica-Ali-DedeI1974;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;"
  }
}
